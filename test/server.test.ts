import { describe, expect, it } from "vitest";

// TODO: Setup tests to be able to test the server, write some tests.
describe("makemeet Server Tests", () => {
  it("2 + 2 works", async () => {
    expect(2 + 2).toBe(4);
  });
});
