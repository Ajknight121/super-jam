// Low priority.
// The properties of a user, to be shown only to that user.
export const prerender = false;

export const GET = () => {
  return new Response("Unimplemented", {
    status: 405,
  });
};
